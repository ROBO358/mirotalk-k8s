apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mirotalk-tls
spec:
  secretName: mirotalk-tls
  commonName: mirotalk.yh.k8s.tsuru.run
  dnsNames:
    - mirotalk.yh.k8s.tsuru.run
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  duration: 2160h
  renewBefore: 720h
